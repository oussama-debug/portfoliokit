{
  "name": "@repo/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "mix phx.server",
    "build": "MIX_ENV=prod mix do compile, assets.deploy",
    "start": "MIX_ENV=prod mix phx.server",
    "test": "mix test",
    "test:watch": "mix test.watch",
    "format": "mix format",
    "lint": "mix format --check-formatted",
    "setup": "mix setup",
    "check-types": "mix dialyzer",
    "db:migrate": "mix ecto.migrate",
    "db:rollback": "mix ecto.rollback",
    "db:reset": "mix ecto.reset",
    "db:seed": "mix run priv/repo/seeds.exs",
    "db:setup": "mix ecto.setup",
    "db:generate": "mix ecto.gen.migration"
  }
}
